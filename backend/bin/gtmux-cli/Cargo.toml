[package]
name = "gtmux-cli"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true

[[bin]]
name = "gtmux"
path = "src/main.rs"

[dependencies]
clap = { workspace = true }
tokio = { workspace = true }
tracing = { workspace = true }
tracing-subscriber = { workspace = true }
anyhow = { workspace = true }
axum = { workspace = true }
gtmux-lifecycle = { path = "../../crates/lifecycle" }
gtmux-ws-server = { path = "../../crates/ws-server" }
gtmux-http-api = { path = "../../crates/http-api" }
gtmux-config = { path = "../../crates/config" }
gtmux-auth = { path = "../../crates/auth" }
gtmux-mux-router = { path = "../../crates/mux-router" }
# libc surfaces `pid_t` for the LIFE-3 pidfile types we accept from the
# lifecycle crate. No signal delivery happens here — that stays inside
# `gtmux_lifecycle::stop_server`.
libc = "0.2"

[dev-dependencies]
tempfile = "3"
