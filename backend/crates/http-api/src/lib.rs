//! gtmux-http-api — axum HTTP router, `GET/PUT /api/layout` + ETag, utoipa
//! OpenAPI spec dump.
//!
//! Bootstrap scaffold only.

#![forbid(unsafe_code)]

/// 부트스트랩 placeholder — HTTP API 라우터 빌더 시그니처.
pub fn router() -> anyhow::Result<()> {
    todo!("http-api::router — axum HTTP router to be implemented")
}
