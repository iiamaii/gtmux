//! gtmux-ws-server — axum WebSocket upgrade + binary envelope codec + MT-3
//! broadcast.
//!
//! Bootstrap scaffold; real WS handler shape sketched in R7 §5.

#![forbid(unsafe_code)]

/// 부트스트랩 placeholder — `/ws` 라우터 빌더 시그니처.
pub fn router() -> anyhow::Result<()> {
    todo!("ws-server::router — axum WS router to be implemented")
}
